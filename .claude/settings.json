{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "cargo fmt --all --manifest-path \"${CLAUDE_PROJECT_DIR:-.}/Cargo.toml\" --check >/dev/null 2>&1 || printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"cargo fmt --check failed. CLAUDE.md requires `cargo fmt` before every commit — run cargo fmt, then commit again.\"}}'",
            "timeout": 60,
            "statusMessage": "Checking rustfmt before commit"
          }
        ]
      }
    ]
  }
}
